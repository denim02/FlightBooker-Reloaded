{
  "name": "flightbooker",
  "private": true,
  "scripts": {
    "install:all": "npm run install:api && npm run install:client && npm run build:client",
    "install:api": "cd api && composer install",
    "install:client": "cd client && npm install",
    "build:client": "cd client && npm run build"
  }
}
